---

- name: ZBX-rebuild-down
  community.docker.docker_compose_v2:
    project_src: "{{ ZBX_HC.path.config }}"
    state: absent

- name: ZBX-rebuild-up
  community.docker.docker_compose_v2:
    project_src: "{{ ZBX_HC.path.config }}"
